Remember this about the codebase: $ARGUMENTS

Parse the input to extract a key and value. Use the MCP remember tool to store it. Default type: convention. Ask for clarification if the input is ambiguous.
