## convention

### typescript-strict-mode
Always use TypeScript strict mode. Enable `"strict": true` in tsconfig.json for all packages.

### explicit-return-types
Return explicit types from all exported functions so callers know the contract at a glance.
